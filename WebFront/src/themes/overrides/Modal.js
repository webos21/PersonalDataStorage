// ==============================|| OVERRIDES - MODAL CELL ||============================== //

export default function Modal(theme) {
    return {
        MuiModal: {
            styleOverrides: {
                root: {
                    // backgroundColor: 'rgba(0, 0, 0, 0.5)',
                    // backdropFilter: 'blur(20px)'
                }
            }
        }
    };
}
